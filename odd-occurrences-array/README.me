# Odd Occurrences Array

Odd Occurrences Array is a task to create a function which accepts an array of odd number of integers, and each element of the array can be paired with another element that has the same value, except for one element that is left unpaired, and to return this unpaired element.

## Install

```
Install NodeJS (https://nodejs.org/en/)
```

## Run

```
node odd-occurrences-array.js (via cmd in the same dir as the file)
```